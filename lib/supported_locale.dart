import 'package:flutter/material.dart';

const supported_locale = [
  const Locale.fromSubtags(languageCode: 'en'), // English
  const Locale.fromSubtags(languageCode: 'de'), // German
  const Locale.fromSubtags(languageCode: 'es'), // Spanish
];
